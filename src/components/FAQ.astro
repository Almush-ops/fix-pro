---
interface FAQItem {
  question: string;
  answer: string;
}

interface Props {
  title?: string;
  subtitle?: string;
  faqs?: FAQItem[];
  includeSchema?: boolean;
}

const {
  title = 'שאלות נפוצות',
  subtitle = 'תשובות לשאלות הנפוצות ביותר',
  faqs = [
    {
      question: 'כמה עולה ניקוי מזגן?',
      answer: 'ניקוי מזגן עילי מתחיל מ-150₪. ניקוי מיני מרכזי ב-450₪ בלבד (במקום 600-700₪ בשוק). המחיר כולל ניקוי פילטרים, חיטוי ובדיקת תקינות מקיפה.',
    },
    {
      question: 'המזגן לא מקרר - מה לעשות?',
      answer: 'מזגן שלא מקרר יכול להעיד על כמה בעיות: חוסר בגז, סתימה בניקוז, פילטר מלוכלך או תקלה במדחס. התקשרו אלינו לאבחון מקצועי - נגיע תוך שעתיים ונזהה את הבעיה במקום.',
    },
    {
      question: 'איך יודעים אם המזגן צריך מילוי גז?',
      answer: 'סימנים לחוסר גז: המזגן עובד אבל לא מקרר מספיק, יש קרח על הצינורות ביחידה החיצונית, או שהמזגן נכבה ונדלק לעתים קרובות. מילוי גז למזגן עולה בין 250-400₪ תלוי בסוג הגז ובכמות.',
    },
    {
      question: 'באילו אזורים אתם עובדים?',
      answer: 'אנחנו מספקים שירות טכנאי מזגנים, אינסטלטור והנדימן בכל אזור חיפה והקריות: חיפה, קריית ביאליק, קריית מוצקין, קריית ים, קריית אתא, נשר, טבעון וזכרון יעקב.',
    },
    {
      question: 'כמה עולה התקנת מזגן?',
      answer: 'התקנת מזגן עילי עם צנרת קצרה מתחילה מ-600₪. המחיר משתנה לפי גודל המזגן, אורך הצנרת והצורך בעבודות חשמל. נגיע לבדיקה ונציע מחיר מדויק לפני העבודה.',
    },
    {
      question: 'האם יש אחריות על העבודה?',
      answer: 'כן! אנחנו נותנים אחריות על כל עבודה: תיקון מזגנים, התקנה, ניקוי, עבודות אינסטלציה והנדימן. אם משהו לא עובד כמו שצריך - אנחנו חוזרים לתקן ללא עלות נוספת.',
    },
  ],
  includeSchema = true,
} = Astro.props;

// Generate FAQ schema
const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: faq.answer.replace(/<[^>]*>/g, ''), // Strip HTML for schema
    },
  })),
};
---

<section class="py-16 md:py-20 bg-surface">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-10">
      <h2 class="text-2xl md:text-3xl font-heading font-bold text-dark mb-3">{title}</h2>
      <p class="text-muted">{subtitle}</p>
    </div>

    <div class="space-y-4">
      {faqs.map((faq, index) => (
        <details class="group bg-white shadow-sm hover:shadow-md border border-border overflow-hidden transition-shadow">
          <summary class="flex items-center justify-between p-5 md:p-6 cursor-pointer list-none hover:bg-accent-50 transition-colors">
            <span class="font-semibold text-dark pe-4">{faq.question}</span>
            <div class="w-8 h-8 bg-accent-100 flex items-center justify-center shrink-0 group-open:bg-accent-500 transition-colors">
              <svg
                class="w-4 h-4 text-accent-500 group-open:text-white transition-all group-open:rotate-180"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </div>
          </summary>
          <div class="accordion-body">
            <div class="px-5 md:px-6 pb-5 md:pb-6 text-muted leading-relaxed border-t border-border/50 pt-4">
              {faq.answer}
            </div>
          </div>
        </details>
      ))}
    </div>
  </div>
</section>

{includeSchema && (
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
)}

<script>
  // Smooth accordion animation
  document.querySelectorAll('details').forEach((details) => {
    const summary = details.querySelector('summary');
    const content = details.querySelector('.accordion-body') as HTMLElement;

    if (!summary || !content) return;

    summary.addEventListener('click', (e) => {
      e.preventDefault();

      if (details.open) {
        // Closing
        content.style.height = content.scrollHeight + 'px';
        requestAnimationFrame(() => {
          content.style.height = '0';
        });
        content.addEventListener('transitionend', () => {
          details.open = false;
          content.style.height = '';
        }, { once: true });
      } else {
        // Opening
        details.open = true;
        const height = content.scrollHeight;
        content.style.height = '0';
        requestAnimationFrame(() => {
          content.style.height = height + 'px';
        });
        content.addEventListener('transitionend', () => {
          content.style.height = '';
        }, { once: true });
      }
    });
  });
</script>
