---
const navItems = [
  { label: 'שירותים', href: '/services/' },
  { label: 'מחירון', href: '/pricing/' },
  { label: 'אזורי שירות', href: '/areas/' },
  { label: 'מדריכים', href: '/blog/' },
  { label: 'אודות', href: '/about/' },
  { label: 'צור קשר', href: '/contact/' },
];

const phone = '055-432-9235';
const phoneHref = 'tel:0554329235';
const currentPath = Astro.url.pathname;

function isActive(href: string): boolean {
  if (href === '/') return currentPath === '/';
  return currentPath.startsWith(href);
}
---

<!-- Main Header - Electrician Style (White with Orange accents) -->
<header class="sticky top-0 z-50 bg-white shadow-sm border-b border-border">
  <nav class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-20 lg:h-24">
      <!-- Logo (right side in RTL) -->
      <a href="/" class="flex items-center">
        <img
          src="/images/logo.svg"
          alt="פיקס פרו - טכנאי מזגנים, אינסטלטור והנדימן"
          class="h-14 lg:h-16 w-auto"
        />
      </a>

      <!-- Desktop: Nav + CTA together (left side in RTL) -->
      <div class="hidden lg:flex items-center gap-8">
        {navItems.map((item) => (
          <a
            href={item.href}
            class:list={[
              'text-sm font-medium transition-colors hover:text-accent-500',
              isActive(item.href) ? 'text-accent-500' : 'text-dark',
            ]}
          >
            {item.label}
          </a>
        ))}
        <a
          href={phoneHref}
          class="flex items-center gap-2 bg-accent-500 text-white px-5 py-2.5 text-sm font-bold hover:bg-accent-600 transition-colors"
        >
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
          </svg>
          <span>התקשרו עכשיו</span>
        </a>
      </div>

      <!-- Mobile Menu Button -->
      <button
        id="mobile-menu-btn"
        class="lg:hidden p-2 text-dark hover:text-accent-500"
        aria-label="פתח תפריט"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>

    <!-- Mobile Navigation -->
    <div id="mobile-menu" class="hidden lg:hidden pb-4 border-t border-border mt-2 pt-4">
      {navItems.map((item) => (
        <a
          href={item.href}
          class:list={[
            'block py-3 text-base font-medium transition-colors hover:text-accent-500 border-b border-border',
            isActive(item.href) ? 'text-accent-500' : 'text-dark',
          ]}
        >
          {item.label}
        </a>
      ))}
      <a
        href={phoneHref}
        class="flex items-center justify-center gap-2 bg-accent-500 text-white px-6 py-3 text-base font-bold hover:bg-accent-600 transition-colors mt-4"
      >
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
          <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
        </svg>
        <span>חייגו עכשיו: {phone}</span>
      </a>
    </div>
  </nav>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  btn?.addEventListener('click', () => menu?.classList.toggle('hidden'));
</script>
